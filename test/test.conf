#include <QPoint>
#include <QString>

Preferences {
    Player {
        QPoint p = QPoint(3,4)
        int mana = 255
        int stamina = 255
        int strength = 10
        QString name = "Josimar"
        Bag {
            int coins = 200
        }
    }
}
